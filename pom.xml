<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.4.1</version>
    <relativePath/>
  </parent>

  <groupId>com.shadhinpay</groupId>
  <artifactId>shadhinpay-parent</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>ShadhinPay Parent</name>
  <description>ShadhinPay backend monorepo (Maven multi-module)</description>

  <modules>
    <module>shadhinpay-common</module>
    <module>shadhinpay-identity</module>
    <module>shadhinpay-provisioning</module>
    <module>shadhinpay-payment-core</module>
    <module>shadhinpay-adapters</module>
    <module>shadhinpay-ledger</module>
    <module>shadhinpay-quota</module>
    <module>shadhinpay-risk</module>
    <module>shadhinpay-invoice</module>
    <module>shadhinpay-settlement</module>
    <module>shadhinpay-application</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Internal version -->
    <shadhinpay.version>0.0.1-SNAPSHOT</shadhinpay.version>

    <!-- Dependency versions -->
    <spring-modulith.version>1.3.1</spring-modulith.version>
    <mapstruct.version>1.6.3</mapstruct.version>
    <lombok.version>1.18.36</lombok.version>
    <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
    <jjwt.version>0.12.6</jjwt.version>
    <resilience4j.version>2.2.0</resilience4j.version>
    <okhttp.version>4.12.0</okhttp.version>
    <testcontainers.version>1.20.4</testcontainers.version>
    <wiremock.version>3.10.0</wiremock.version>
    <jqwik.version>1.9.2</jqwik.version>
    <archunit.version>1.3.0</archunit.version>
    <rest-assured.version>5.5.0</rest-assured.version>
    <springdoc.version>2.7.0</springdoc.version>

    <!-- Plugin versions -->
    <spotless.version>2.43.0</spotless.version>
    <google-java-format.version>1.24.0</google-java-format.version>
    <jacoco.version>0.8.12</jacoco.version>
    <spotbugs-maven-plugin.version>4.8.6.6</spotbugs-maven-plugin.version>
    <maven-pmd-plugin.version>3.26.0</maven-pmd-plugin.version>
    <springdoc-openapi-maven-plugin.version>1.4</springdoc-openapi-maven-plugin.version>

    <!-- Coverage thresholds: keep in lockstep with DEVELOPMENT_WORKFLOW.md §8. -->
    <jacoco.line.coverage.minimum>0.80</jacoco.line.coverage.minimum>
    <jacoco.branch.coverage.minimum>0.70</jacoco.branch.coverage.minimum>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Internal modules -->
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-common</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-identity</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-provisioning</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-payment-core</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-adapters</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-ledger</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-quota</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-risk</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-invoice</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>
      <dependency>
        <groupId>com.shadhinpay</groupId>
        <artifactId>shadhinpay-settlement</artifactId>
        <version>${shadhinpay.version}</version>
      </dependency>

      <!-- Spring Modulith BOM -->
      <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-bom</artifactId>
        <version>${spring-modulith.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Resilience4j BOM -->
      <dependency>
        <groupId>io.github.resilience4j</groupId>
        <artifactId>resilience4j-bom</artifactId>
        <version>${resilience4j.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Testcontainers BOM -->
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- MapStruct -->
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct</artifactId>
        <version>${mapstruct.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct-processor</artifactId>
        <version>${mapstruct.version}</version>
      </dependency>
      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok-mapstruct-binding</artifactId>
        <version>${lombok-mapstruct-binding.version}</version>
      </dependency>

      <!-- JJWT -->
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>${jjwt.version}</version>
      </dependency>

      <!-- OkHttp -->
      <dependency>
        <groupId>com.squareup.okhttp3</groupId>
        <artifactId>okhttp</artifactId>
        <version>${okhttp.version}</version>
      </dependency>

      <!-- WireMock -->
      <dependency>
        <groupId>org.wiremock</groupId>
        <artifactId>wiremock-jetty12</artifactId>
        <version>${wiremock.version}</version>
      </dependency>

      <!-- jqwik -->
      <dependency>
        <groupId>net.jqwik</groupId>
        <artifactId>jqwik</artifactId>
        <version>${jqwik.version}</version>
      </dependency>

      <!-- ArchUnit -->
      <dependency>
        <groupId>com.tngtech.archunit</groupId>
        <artifactId>archunit-junit5</artifactId>
        <version>${archunit.version}</version>
      </dependency>

      <!-- REST Assured -->
      <dependency>
        <groupId>io.rest-assured</groupId>
        <artifactId>rest-assured</artifactId>
        <version>${rest-assured.version}</version>
      </dependency>

      <!-- Springdoc OpenAPI -->
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>${springdoc.version}</version>
      </dependency>

      <!-- H2 (only used by the `openapi` profile to boot the app without Postgres). -->
      <dependency>
        <groupId>com.h2database</groupId>
        <artifactId>h2</artifactId>
        <version>2.3.232</version>
      </dependency>
      <!-- Spring Retry & Aspects -->
      <dependency>
        <groupId>org.springframework.retry</groupId>
        <artifactId>spring-retry</artifactId>
        <version>2.0.11</version>
      </dependency>
      <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-aspects</artifactId>
        <version>6.1.14</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <release>${java.version}</release>
            <annotationProcessorPaths>
              <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
              </path>
              <path>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
              </path>
              <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok-mapstruct-binding</artifactId>
                <version>${lombok-mapstruct-binding.version}</version>
              </path>
            </annotationProcessorPaths>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <configuration>
            <source>${java.version}</source>
            <doclint>all,-missing</doclint>
            <failOnError>false</failOnError>
            <quiet>true</quiet>
            <detectJavaApiLink>false</detectJavaApiLink>
          </configuration>
        </plugin>

        <!--
          JaCoCo: agent + report run on every build so devs always see coverage in target/site/jacoco/.
          The hard 80%-line / 70%-branch gate (`check` execution) is gated behind the `coverage`
          profile and runs in CI — see DEVELOPMENT_WORKFLOW.md §8.
        -->
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>${jacoco.version}</version>
          <executions>
            <execution>
              <id>jacoco-prepare-agent</id>
              <goals>
                <goal>prepare-agent</goal>
              </goals>
            </execution>
            <execution>
              <id>jacoco-report</id>
              <goals>
                <goal>report</goal>
              </goals>
              <phase>verify</phase>
            </execution>
          </executions>
        </plugin>

        <!-- SpotBugs static analysis -->
        <plugin>
          <groupId>com.github.spotbugs</groupId>
          <artifactId>spotbugs-maven-plugin</artifactId>
          <version>${spotbugs-maven-plugin.version}</version>
          <configuration>
            <effort>Max</effort>
            <threshold>Medium</threshold>
            <failOnError>true</failOnError>
            <includeTests>false</includeTests>
            <excludeFilterFile>${maven.multiModuleProjectDirectory}/config/spotbugs/spotbugs-exclude.xml</excludeFilterFile>
          </configuration>
        </plugin>

        <!-- PMD static analysis -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-pmd-plugin</artifactId>
          <version>${maven-pmd-plugin.version}</version>
          <configuration>
            <printFailingErrors>true</printFailingErrors>
            <failOnViolation>true</failOnViolation>
            <linkXRef>false</linkXRef>
            <includeTests>false</includeTests>
            <minimumTokens>100</minimumTokens>
            <targetJdk>${java.version}</targetJdk>
            <rulesets>
              <ruleset>${maven.multiModuleProjectDirectory}/config/pmd/ruleset.xml</ruleset>
            </rulesets>
          </configuration>
        </plugin>

        <!-- springdoc-openapi runtime dump (used in shadhinpay-application). -->
        <plugin>
          <groupId>org.springdoc</groupId>
          <artifactId>springdoc-openapi-maven-plugin</artifactId>
          <version>${springdoc-openapi-maven-plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.version}</version>
        <configuration>
          <java>
            <includes>
              <include>src/main/java/**/*.java</include>
              <include>src/test/java/**/*.java</include>
            </includes>
            <googleJavaFormat>
              <version>${google-java-format.version}</version>
              <style>GOOGLE</style>
              <reflowLongStrings>true</reflowLongStrings>
            </googleJavaFormat>
            <removeUnusedImports/>
            <trimTrailingWhitespace/>
            <endWithNewline/>
          </java>
          <pom>
            <includes>
              <include>pom.xml</include>
            </includes>
            <sortPom>
              <expandEmptyElements>false</expandEmptyElements>
            </sortPom>
          </pom>
        </configuration>
        <executions>
          <execution>
            <id>spotless-check</id>
            <goals>
              <goal>check</goal>
            </goals>
            <phase>verify</phase>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Coverage gate. CI activates this with `-Pcoverage` so jacoco:check hard-fails the build
      when any module falls below the thresholds defined in `jacoco.line.coverage.minimum` /
      `jacoco.branch.coverage.minimum`. Local runs default off so devs still see coverage
      reports in target/site/jacoco/ without the gate blocking iterative work.
    -->
    <profile>
      <id>coverage</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>jacoco-check</id>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>verify</phase>
                <configuration>
                  <haltOnFailure>true</haltOnFailure>
                  <rules>
                    <rule>
                      <element>BUNDLE</element>
                      <limits>
                        <limit>
                          <counter>LINE</counter>
                          <value>COVEREDRATIO</value>
                          <minimum>${jacoco.line.coverage.minimum}</minimum>
                        </limit>
                        <limit>
                          <counter>BRANCH</counter>
                          <value>COVEREDRATIO</value>
                          <minimum>${jacoco.branch.coverage.minimum}</minimum>
                        </limit>
                      </limits>
                    </rule>
                  </rules>
                  <excludes>
                    <exclude>**/generated/**</exclude>
                    <exclude>**/dto/**</exclude>
                    <exclude>**/*Application.class</exclude>
                    <exclude>**/package-info.class</exclude>
                    <exclude>**/config/OpenApiSecurityConfig.class</exclude>
                  </excludes>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!--
      Static analysis gate. CI activates this with `-Pstatic-analysis` so SpotBugs (`check`)
      and PMD (`check`) hard-fail the build. Local builds may run the analyses on demand
      with `mvn spotbugs:check pmd:check` without enabling this profile.
    -->
    <profile>
      <id>static-analysis</id>
      <build>
        <plugins>
          <plugin>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>spotbugs-check</id>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>verify</phase>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-pmd-plugin</artifactId>
            <executions>
              <execution>
                <id>pmd-check</id>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>verify</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
