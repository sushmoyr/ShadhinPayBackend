<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.shadhinpay</groupId>
    <artifactId>shadhinpay-parent</artifactId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>

  <artifactId>shadhinpay-application</artifactId>
  <name>ShadhinPay :: Application</name>
  <description>Spring Boot bootstrap module — wires every feature module</description>

  <properties>
    <openapi.server.port>8089</openapi.server.port>
  </properties>

  <dependencies>
    <!-- Internal feature modules -->
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-common</artifactId>
    </dependency>
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-identity</artifactId>
    </dependency>
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-provisioning</artifactId>
    </dependency>
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-payment-core</artifactId>
    </dependency>
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-adapters</artifactId>
    </dependency>
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-ledger</artifactId>
    </dependency>
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-quota</artifactId>
    </dependency>
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-risk</artifactId>
    </dependency>
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-invoice</artifactId>
    </dependency>
    <dependency>
      <groupId>com.shadhinpay</groupId>
      <artifactId>shadhinpay-settlement</artifactId>
    </dependency>

    <!-- Spring Boot starters needed by the bootstrap -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>

    <!-- Modulith JDBC event publication registry + observability -->
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-starter-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-starter-jdbc</artifactId>
    </dependency>

    <!-- DB drivers + migrations -->
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- OpenAPI / Swagger UI -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Tests -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-jetty12</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.github.hakky54</groupId>
      <artifactId>logcaptor</artifactId>
      <version>2.9.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>com.shadhinpay.ShadhinPayApplication</mainClass>
        </configuration>
        <executions>
          <execution>
            <id>repackage</id>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Opt-in OpenAPI emission. Activated with `-Popenapi` (or by CI). Boots the application
      under the `openapi` Spring profile (H2-backed, Flyway off) just long enough for the
      springdoc-openapi-maven-plugin to scrape /v3/api-docs and write target/openapi/openapi.json.
    -->
    <profile>
      <id>openapi</id>
      <dependencies>
        <dependency>
          <groupId>com.h2database</groupId>
          <artifactId>h2</artifactId>
          <scope>runtime</scope>
        </dependency>
      </dependencies>
      <build>
        <plugins>
          <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>pre-integration-test</id>
                <goals>
                  <goal>start</goal>
                </goals>
                <phase>pre-integration-test</phase>
                <configuration>
                  <jvmArguments>-Dspring.profiles.active=openapi -Dserver.port=${openapi.server.port}</jvmArguments>
                  <wait>1000</wait>
                  <maxAttempts>60</maxAttempts>
                </configuration>
              </execution>
              <execution>
                <id>post-integration-test</id>
                <goals>
                  <goal>stop</goal>
                </goals>
                <phase>post-integration-test</phase>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>generate-openapi</id>
                <goals>
                  <goal>generate</goal>
                </goals>
                <phase>integration-test</phase>
                <configuration>
                  <apiDocsUrl>http://localhost:${openapi.server.port}/v3/api-docs</apiDocsUrl>
                  <outputFileName>openapi.json</outputFileName>
                  <outputDir>${project.build.directory}/openapi</outputDir>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
